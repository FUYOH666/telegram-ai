# Руководство по внесению вклада

Спасибо за интерес к проекту Telegram AI Assistant Platform! Мы рады вашему вкладу.

## Как внести вклад

### Сообщение о проблеме

Если вы нашли баг или у вас есть предложение по улучшению:

1. Проверьте, нет ли уже открытого Issue с такой же проблемой
2. Создайте новый Issue с подробным описанием:
   - Что произошло (для багов)
   - Что вы ожидали (для багов)
   - Шаги для воспроизведения (для багов)
   - Предложение решения (для улучшений)

### Внесение изменений

1. **Fork репозиторий**
   ```bash
   # Нажмите кнопку Fork на GitHub
   ```

2. **Клонируйте ваш fork**
   ```bash
   git clone https://github.com/YOUR_USERNAME/telegram-ai.git
   cd telegram-ai
   ```

3. **Создайте ветку для вашей функции**
   ```bash
   git checkout -b feature/your-feature-name
   # или для багфикса:
   git checkout -b fix/your-bug-fix
   ```

4. **Установите зависимости**
   ```bash
   uv sync
   ```

5. **Внесите изменения**
   - Следуйте стилю кода проекта
   - Добавляйте тесты для новых функций
   - Обновляйте документацию при необходимости

6. **Проверьте код**
   ```bash
   # Форматирование
   uv run ruff format .
   
   # Линтинг
   uv run ruff check .
   
   # Проверка типов
   uv run pyright
   
   # Тесты
   uv run pytest
   ```

7. **Закоммитьте изменения**
   ```bash
   git add .
   git commit -m "feat: add amazing feature"
   # Используйте префиксы: feat, fix, docs, style, refactor, test, chore
   ```

8. **Запушьте в ваш fork**
   ```bash
   git push origin feature/your-feature-name
   ```

9. **Создайте Pull Request**
   - Перейдите на GitHub
   - Нажмите "New Pull Request"
   - Опишите ваши изменения
   - Укажите связанные Issues (если есть)

## Стандарты кода

### Python

- Используйте Python 3.12+
- Следуйте PEP 8 (проверяется через ruff)
- Используйте type hints где возможно
- Добавляйте docstrings для публичных функций

### Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации и т.д.

### Тесты

- Добавляйте тесты для новых функций
- Стремитесь к покрытию кода ≥70%
- Используйте pytest для тестирования

## Структура проекта

```
telegram-ai/
├── services/          # Микросервисы
├── shared/            # Общие модули
├── src/               # Основной код
├── tests/             # Тесты
├── scripts/           # Утилиты
└── infrastructure/   # Docker, K8s
```

## Вопросы?

Если у вас есть вопросы, создайте Issue или свяжитесь с нами:

- **Telegram**: [@ScanovichAI](https://t.me/ScanovichAI)
- **Email**: iamfuyoh@gmail.com

Спасибо за ваш вклад! 🎉

